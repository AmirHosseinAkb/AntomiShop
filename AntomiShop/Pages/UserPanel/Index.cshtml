@page
@model AntomiShop.Pages.UserPanel.IndexModel

@inject IUserService _userService
@{
    Layout = "_DefaultLayout";
    var user = _userService.GetUserPanelInformations(User.Identity.Name);
}

<div class="breadcrumbs_area">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="breadcrumb_content">
                    <ul>
                        <li><a href="/">خانه</a></li>
                        <li>حساب کاربری</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="account_page_bg">
    <div class="container">
        <section class="main_content_area">
            <div class="account_dashboard">
                <div class="row">
                    <partial name="_SideBar" />
                    <div class="col-sm-12 col-md-9 col-lg-9">
                        <!-- Tab panes -->
                        <div class="tab-content dashboard_content">
                            <div class="tab-pane fade show active" id="dashboard">
                                <div class="card">
                                    <div class="card-header">
                                        <p class="card-title">مشخصات کاربری</p>
                                    </div>
                                    <div class="card-body">
                                        @if (user.FullName.Trim().Length != 0)
                                        {
                                            <p>نام و نام خانوادگی : @user.FullName</p>
                                        }
                                        <p>ایمیل : @user.Email</p>
                                        @if (!string.IsNullOrEmpty(user.PhoneNumber))
                                        {
                                            <p>شماره همراه : @user.PhoneNumber</p>
                                        }
                                        <p>تاریخ ثبت نام : @user.RegisterDate.ConvertToShamsi()</p>
                                        <p>موجودی کیف پول : @user.WalletBalance.ToString("#,0") تومان</p>
                                        @if (user.FullName.Trim().Length == 0)
                                        {
                                            <hr />
                                            <span class="btn btn-info">لطفا از طریق بخش جزئیات حساب، اطلاعات کاربری خود را تکمیل نمایید</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>






