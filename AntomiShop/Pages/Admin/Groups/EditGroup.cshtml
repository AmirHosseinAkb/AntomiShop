@page
@model AntomiShop.Pages.Admin.Groups.EditGroupModel
@{
}

@*<div class="row">
    <form role="form" method="post" enctype="multipart/form-data">
        <div class="col-md-9" style="height:650px;overflow-y:scroll">
            <div class="panel panel-default">
                <div class="panel-heading"><h3 class="panel-title">افزودن گروه جدید</h3></div>
                <div class="panel-body">
                    <input type="hidden" asp-for="ProductGroup.ParentId" />
                    <div class="form-group">
                        <label>عنوان گروه </label>
                        <input asp-for="ProductGroup.GroupTitle" type="text" class="form-control" id="exampleInputEmail1">
                    </div>
                    <button type="submit" class="btn btn-purple waves-effect waves-light">ثبت گروه</button>
                </div><!-- panel-body -->
            </div> <!-- panel -->
        </div>
        @if (Model.ProductGroup.ParentId == null)
        {
            <div class="col-md-3">
                <div class="panel panel-default">
                    <div class="panel-heading"><h3 class="panel-title">تصویر گروه</h3></div>
                    <div class="panel-body">
                        <div class="form-group">
                            <img id="imgAvatar" class="img img-thumbnail" style="width:100%;height:100%" src="/Groups/Group.png" />
                        </div>
                        <div class="form-group">
                            <input name="groupPic" id="imgInp" type="file" class="form-control" />
                        </div>
                    </div>
                </div>
            </div>
        }
    </form>
</div>
*@
@section Scripts
{
<script>
    function readURL(input) {

        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#imgAvatar').attr('src', e.target.result);
            }

            reader.readAsDataURL(input.files[0]);
        }
    }
    $("#imgInp").change(function () {
        readURL(this);
    });
</script>
<script src="https://cdn.ckeditor.com/4.9.2/standard/ckeditor.js"></script>
<script>
    CKEDITOR.replace('Product_ProductDescription', {
        customConfig: '/js/Config.js'
    });
</script>
}